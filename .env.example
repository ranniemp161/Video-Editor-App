# AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# Security: App authentication password (REQUIRED)
APP_PASSWORD=change_me_to_a_strong_password

# Security: JWT signing secret (REQUIRED — generate with: python -c "import secrets; print(secrets.token_hex(32))")
JWT_SECRET=change_me_generate_with_secrets_token_hex

# Security Limits
MAX_UPLOAD_SIZE_MB=500
